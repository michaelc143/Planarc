export interface AuthResponse {
    date_joined: string;
    email: string;
    message: string;
    username: string;
    user_id: string;
}

export interface UsernameResponse {
    message: string;
    username: string;
}